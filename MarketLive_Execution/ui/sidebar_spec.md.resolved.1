# Sidebar Specification (Emoji Edition)

## Structure

### Top
- Logo: **freightcode®** (Text only or with 🟦 block)

### Platform (Core)
- 📊 **Dashboard** (`/`)
- 📦 **Shipments** (`/shipments`)
- 🧾 **Quotes** (`/quotes`)
- 🚢 **Bookings** (`/bookings`)
- 📍 **GeoRisk** (`/georisk`) *[PREMIUM]*

### Operations
- 📂 **Documents** (`/documents`)
- ⚠️ **Compliance** (`/compliance`)
- 💰 **Finance** (`/payments`)

### Admin / Settings
- 👥 **Users** (`/admin/users`)
- ⚙️ **Settings** (`/settings`)
- 🔌 **API** (`/api-docs`)

## Behavior
- **Hover**: `bg-slate-800`
- **Active**: `bg-blue-600 shadow-lg`
- **Collapsed**: Emojis only centered.
