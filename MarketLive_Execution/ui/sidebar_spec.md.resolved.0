# Sidebar Navigation Specification

**Style:** Fixed, Dark Mode (Deep Navy), High Density.
**Typography:** `text-sm`, `font-medium`, `text-slate-300` (inactive), `text-white` (active).

## Menu Structure

### 🌐 Main
*   📊 **Dashboard** (`/dashboard`)
*   📦 **Shipments** (`/shipments`)
*   📝 **Quotes** (`/quotes`)
*   📍 **GeoRisk Navigator™** (`/georisk`) *[Badge: PRO]*

### 💼 Operations
*   🧾 **Documents** (`/documents`)
*   ⚠️ **Compliance** (`/compliance`)
*   💸 **Payments** (`/payments`)

### 🛠️ Admin
*   🧠 **AI Config** (`/ai-config`)
*   ⚙️ **Settings** (`/settings`)
*   👥 **Users** (`/users`)

## Implementation Notes (Tailwind)
```jsx
<nav className="w-64 bg-slate-900 h-screen p-4 flex flex-col gap-1 border-r border-slate-800">
  <div className="text-xl font-bold text-white mb-6 px-2">MarketLive 🚢</div>
  
  {/* Detailed Item */}
  <a href="/shipments" className="flex items-center gap-3 px-3 py-2 text-sm text-slate-300 hover:bg-slate-800 rounded-md transition-colors">
    <span>📦</span>
    <span>Shipments</span>
  </a>

  {/* Active Item */}
  <a href="/georisk" className="flex items-center gap-3 px-3 py-2 text-sm text-white bg-blue-900/30 border-l-2 border-blue-400 rounded-r-md">
    <span>📍</span>
    <span>GeoRisk Navigator™</span>
    <span className="ml-auto text-[10px] bg-blue-500/20 text-blue-300 px-1.5 py-0.5 rounded">PRO</span>
  </a>
</nav>
```
